attempt_to_break_entrancement = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_realm.dds"
	}
	decision_group_type = elf

	desc = "I've been under control of a vicious creature for far too long! It's time to break free and regain my freedom!"
	selection_tooltip = "Attempt to break from Entrancement"

	ai_goal = yes
	ai_will_do = {
		value = 100
	}
	ai_potential = {
		
	}
	cooldown = { years = 1 }
	
	# ai_check_interval = 4

	is_shown = {
		has_trait = entranced	
	}
	
	# Following conditions must be true to take decision, shown in requirements section
	is_valid = {
		has_trait = entranced		
	}

	effect = {
		save_scope_as = actor
		
		random_living_character = {
			limit = {
				has_hook_of_type = {
					target = scope:actor
					type = entranced
					}
				}		
			save_scope_as = recipient
		}

		trigger_event = { 
			id = entrance.1160
		}	
	}
}