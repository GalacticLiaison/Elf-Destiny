# Flying unit game concepts and troop-type textures

# General concepts
flying_units = {}
# flying_warbeast = {}

# --- MAA unit declarations (so tools and references can point to them) ---
thundarael_griffin_riders_maa = { parent = men_at_arms }
griffin_knights_maa = { parent = men_at_arms }
honored_drake_masters_maa = { parent = men_at_arms }

# --- Troop types with textures ---
# Honored Drake Masters (drake-mounted heavy lancers)
honored_drake_masters = {
    parent = men_at_arms
    texture = "gfx/interface/icons/regimenttypes/honored_drake_masters.dds"
}

# Thundarael Griffin Riders (elite aerial cavalry)
thundarael_griffin_riders = {
    parent = men_at_arms
    texture = "gfx/interface/icons/regimenttypes/thundarael_griffin_riders.dds"
}

# Griffin Knights (heavy aerial shock troops)
griffin_knights = {
    parent = men_at_arms
    texture = "gfx/interface/icons/regimenttypes/griffin_knights.dds"
}

# --- Optional special troop categories (for flavor/forces) ---
griffin_force = {
    parent = special_troops
    texture = "gfx/interface/icons/regimenttypes/griffin_knights.dds"
}

# Notes:
# - Use the `*_maa` keys above when referencing the unit as a men_at_arms group in events/decisions.
# - Texture paths should match files under `gfx/interface/icons/regimenttypes/`.
# - Localization keys for display text are defined in `localization/english/` (see `royal_elf_bloodlines_maa_l_english.yml`).
