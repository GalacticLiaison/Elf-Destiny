# Scope: Character
GH_must_use_statue_effect_gold = {
	is_alive = yes
	has_variable = GH_is_gold_statue
}

# Scope: Character
GH_must_use_statue_effect_marble = {
	is_alive = yes
	has_variable = GH_is_marble_statue
}

# Scope: Character
GH_must_use_statue_effect_limestone = {
	is_alive = yes
	has_variable = GH_is_limestone_statue
}

# Scope: Character
GH_must_use_statue_effect_stone = {
	is_alive = yes
	has_variable = GH_is_stone_statue
}

# Scope: Character
GH_must_use_statue_effect_copper = {
	is_alive = yes
	has_variable = GH_is_copper_statue
}

# Scope: Character
GH_must_use_statue_effect_copper_rust = {
	is_alive = yes
	has_variable = GH_is_copper_rust_statue
}

GH_must_use_statue_effect_silver = {
	is_alive = yes
	has_variable = GH_is_silver_statue
}
GH_must_use_statue_effect_iron = {
	is_alive = yes
	has_variable = GH_is_iron_statue
}
GH_must_use_statue_effect_lead = {
	is_alive = yes
	has_variable = GH_is_lead_statue
}
GH_must_use_statue_effect_mercury = {
	is_alive = yes
	has_variable = GH_is_mercury_statue
}
GH_must_use_statue_effect_blood = {
	is_alive = yes
	has_variable = GH_is_blood_statue
}
GH_must_use_statue_effect_obsidian = {
	is_alive = yes
	has_variable = GH_is_obsidian_statue
}
GH_must_use_statue_effect_shadow = {
	is_alive = yes
	has_variable = GH_is_shadow_statue
}
GH_must_use_statue_effect_greenjade = {
	is_alive = yes
	has_variable = GH_is_greenjade_statue
}
GH_must_use_statue_effect_ethereal = {
	is_alive = yes
	has_variable = GH_is_ethereal_statue
}
GH_must_use_statue_effect_water = {
	is_alive = yes
	has_variable = GH_is_water_statue
}
GH_must_use_statue_effect_light = {
	is_alive = yes
	has_variable = GH_is_light_statue
}
GH_must_use_statue_effect_dust = {
	is_alive = yes
	has_variable = GH_is_dust_statue
}